# Pre-Mortem Analysis: Gap Detector & Meta-Learning Integration

## Executive Summary

**Scenario:** 6 months post-deployment, the Gap Detector and Meta-Learning system has failed. This document analyzes why it happened, categorizes the failure modes, and provides actionable prevention strategies.

---

## Топ-10 Причин Провала (Приоритизированные)

### 🔴 Критический Риск (Предотвратить немедленно)

#### 1. Порог confidence=0.6 — "One Size Fits None"
**Категория:** Архитектурная / Алгоритмическая

**Как проявится:**
- False positives: 60% простых вопросов попадают в очередь изучения
- False negatives: 55% сложных технических вопросов пропускаются
- Очередь разрастается до 500+ тем за месяц
- Deep Learning не справляется с нагрузкой

**Когда проявится:**
- Неделя 2-3: Пользователь замечает "странные" темы в очереди
- Месяц 2: Очередь переполнена, система "захлебывается"
- Месяц 3: Пользователь отключает auto-extract

**Каскадные эффекты:**
```
Неправильный threshold → Много false positives → 
→ Очередь переполнена → Deep Learning перегружен → 
→ Качество знаний падает → Пользователь теряет доверие → 
→ Система отключена
```

**Ранние сигналы:**
- Соотношение добавленных/использованных тем > 10:1
- Среднее время в очереди > 30 дней
- Пользователь вручную удаляет темы из очереди

**Как предотвратить:**
```javascript
// Вместо фиксированного threshold:
class AdaptiveThreshold {
  constructor() {
    this.baseThreshold = 0.6;
    this.questionComplexity = new Map(); // topic -> complexity score
  }
  
  getThreshold(session) {
    const complexity = this.assessComplexity(session);
    // Сложные вопросы: lower threshold (ловим gaps)
    // Простые вопросы: higher threshold (избегаем false positives)
    return this.baseThreshold + (complexity - 0.5) * 0.3;
  }
  
  assessComplexity(session) {
    const indicators = {
      codeBlocks: (session.text.match(/```/g) || []).length / 2,
      technicalTerms: this.countTechnicalTerms(session.text),
      questionDepth: session.followUpCount / 10,
      contextLength: Math.min(session.text.length / 5000, 1)
    };
    return weightedAverage(indicators);
  }
}
```

**MVP-проверка:**
```javascript
// Запустить на истории сессий за неделю
// Посчитать: сколько gaps найдено vs сколько реально нужно было
// Подобрать threshold так, чтобы precision > 0.7
```

---

#### 2. Метрика "использования темы" коррелирует, но не предсказывает
**Категория:** Методологическая / Data Science

**Как проявится:**
- Meta-Learning рапортует: "Тема X используется часто → успешная"
- Реальность: Тема X упоминается часто, потому что вызывает проблемы
- Система рекомендует "изучить больше похожих тем"
- Пользователь получает знания о проблемных технологиях

**Когда проявится:**
- Месяц 2-3: Отчёты Meta-Learning противоречат интуиции
- Месяц 4: Пользователь перестаёт доверять рекомендациям
- Месяц 6: Функция отключена

**Каскадные эффекты:**
```
Неправильная метрика → Ложные рекомендации → 
→ Время тратится на неправильные темы → 
→ Gap не закрываются → Система бесполезна
```

**Ранние сигналы:**
- Темы с "высоким использованием" часто связаны с ошибками
- Пользователь не кликает на рекомендации
- Рекомендации дублируют то, что уже известно

**Как предотвратить:**
```javascript
// Многомерная метрика вместо частоты
class TopicValueMetric {
  calculate(topic) {
    return {
      // Частота НЕ равно ценность
      mentionFrequency: this.getMentions(topic),
      
      // Насколько помогло знание?
      problemResolutionRate: this.getResolutionRate(topic),
      
      // Уменьшилось ли количество follow-up вопросов?
      followUpReduction: this.getFollowUpDelta(topic),
      
      // Явная обратная связь
      explicitFeedback: this.getFeedbackScore(topic),
      
      // Время от вопроса до решения
      timeToResolution: this.getResolutionTime(topic)
    };
  }
  
  // Взвешенная оценка
  getValue(score) {
    return (
      score.problemResolutionRate * 0.35 +
      score.followUpReduction * 0.25 +
      score.explicitFeedback * 0.25 +
      (1 / score.timeToResolution) * 0.15
    );
  }
}
```

**MVP-проверка:**
- Вручную разметить 50 сессий: "эта тема помогла решить проблему?"
- Сравнить корреляцию частоты vs problemResolution с разметкой

---

### 🟠 Высокий Риск (Сложно предотвратить → Искать альтернативы)

#### 3. Feedback Loop Слишком Длинный
**Категория:** Архитектурная / Производительность

**Как проявится:**
- Gap обнаружен → Добавлен в очередь → Deep Learning обрабатывает ночью → 
- Знание доступно на следующий день → Пользователь уже забыл вопрос
- Пользователь: "Зачем мне это? Я уже разобрался"
- Система "отстаёт от реальности" на 1-2 дня

**Когда проявится:**
- Неделя 1: Незаметно
- Месяц 2-3: Пользователь замечает несвежие рекомендации
- Месяц 4-6: Полное игнорирование рекомендаций

**Каскадные эффекты:**
```
Задержка 24ч → Знания не релевантны → Игнорирование → 
→ Метрики использования падают → Meta-Learning считает темы бесполезными → 
→ Перекос в рекомендациях
```

**Ранние сигналы:**
- Время от detect до использования > 48 часов
- Click-through rate на рекомендации < 5%
- Пользователь гуглит вместо использования Knowledge Search

**Как предотвратить (альтернативы):**

**Вариант A: Инкрементальное обучение**
```javascript
// Не ждать ночного batch, а учить сразу
class IncrementalGapLearning {
  async onGapDetected(gap) {
    // Быстрый "placeholder" сразу
    await this.createStub(gap);
    
    // Фоновое обогащение
    this.enrichInBackground(gap).then(enriched => {
      this.updateStub(enriched);
    });
  }
  
  createStub(gap) {
    // Минимально полезная информация за 2-3 секунды
    return {
      name: gap.topic,
      type: 'stub',
      description: `Learning about ${gap.topic}...`,
      quickLinks: this.fetchQuickLinks(gap.topic), // API Wikipedia/MDN/etc
      availableAt: Date.now() + 5000
    };
  }
}
```

**Вариант B: Predictive Pre-Learning**
```javascript
// Учить ТО, что ВЕРОЯТНО понадобится
class PredictivePreLearning {
  async predictAndPrelearn(session) {
    // На основе текущего разговора предсказать gaps
    const predictedGaps = await this.predictGaps(session);
    
    // Запустить lightweight learning сразу
    for (const gap of predictedGaps) {
      if (gap.confidence > 0.7) {
        this.triggerImmediateLearning(gap);
      }
    }
  }
}
```

**MVP-проверка:**
- Измерить текущий цикл: detect → available
- Целевое время: < 5 минут для stub, < 1 час для полного

---

#### 4. Локальные Максимумы в Стратегии Обучения
**Категория:** Алгоритмическая / ML

**Как проявится:**
- Meta-Learning находит "успешную" стратегию: "учить всё про Docker"
- Система учит 50 статей про Docker
- Пользователю нужно Kubernetes, но система "зациклилась"
- Docker знания уже избыточны, Kubernetes gaps не закрыты

**Когда проявится:**
- Месяц 3-4: Пользователь замечает дублирование
- Месяц 5-6: Knowledge base перекошена, непокрытые области растут

**Каскадные эффекты:**
```
Оптимизация локального максимума → Избыточность в одной области → 
→ Недостаток в других → Пользователь недоволен → 
→ Ручное вмешательство требуется постоянно
```

**Ранние сигналы:**
- Темы одного типа доминируют в KB (>30%)
- Coverage метрика по категориям неравномерная
- Diversity score падает

**Как предотвратить:**
```javascript
class DiversityEnforcingStrategy {
  async recommendStrategy() {
    const currentCoverage = await this.getCategoryCoverage();
    const gaps = await this.findPersistentGaps();
    
    // Exploration vs Exploitation
    const strategy = {
      // 60%: Закрывать реальные gaps
      exploitation: this.selectTopGaps(gaps, 0.6),
      
      // 30%: Поддерживать баланс категорий
      categoryBalance: this.selectUnderrepresented(currentCoverage, 0.3),
      
      // 10%: Случайное исследование
      exploration: this.selectRandomNew(0.1)
    };
    
    return strategy;
  }
  
  // Запретить более N тем одной категории подряд
  async selectNextTopic(queue) {
    const recentCategories = await this.getRecentCategories(10);
    
    for (const topic of queue) {
      const category = this.getCategory(topic);
      const categoryCount = recentCategories.filter(c => c === category).length;
      
      if (categoryCount < 3) { // Не более 3 подряд
        return topic;
      }
    }
    
    // Если все категории "переполнены", взять из underrepresented
    return this.getUnderrepresentedTopic();
  }
}
```

---

### 🟡 Средний Риск (Мониторинг + Постепенное улучшение)

#### 5. Неточное Извлечение Намерений
**Категория:** NLP / Техническая

**Как проявится:**
- Пользователь: "Я НЕ ЗНАЮ React" → Intent Detector: "Добавлено: React"
- Пользователь: "Давай НЕ будем использовать MongoDB" → Добавлено: MongoDB
- Отрицания и контекст игнорируются

**Когда проявится:**
- Неделя 1-2: Сразу видны "странные" добавления

**Как предотвратить:**
```javascript
class ContextAwareIntentExtractor {
  async extract(session) {
    const sentiment = await this.analyzeSentiment(session);
    const negations = this.findNegations(session.text);
    
    // Инвертировать intent при отрицании
    for (const intent of rawIntents) {
      const hasNegation = negations.some(n => 
        this.isNear(intent.position, n.position, 5) // 5 слов
      );
      
      if (hasNegation && sentiment.negative) {
        intent.type = 'avoidance'; // Вместо 'learning_intent'
        intent.priority = 'low';
      }
    }
  }
}
```

---

#### 6. Циркулярные Зависимости в Meta-Learning
**Категория:** Архитектурная

**Как проявится:**
- Meta-Learning анализирует эффективность → Gap Detector использует результаты → 
- Gap Detector добавляет темы → Deep Learning создаёт знания → 
- Meta-Learning видит новые знания → Анализирует эффективность → ...
- Система "крутится в холостую", тратя ресурсы

**Когда проявится:**
- Месяц 2-4: CPU/memory usage растёт непропорционально

**Как предотвратить:**
```javascript
// Чёткое разделение фаз
const LEARNING_PHASES = {
  DETECT: '00-06',    // Ночь: только обнаружение
  LEARN: '06-12',     // Утро: Deep Learning
  ANALYZE: '12-18',   // День: Meta-Learning анализ
  UPDATE: '18-24'     // Вечер: обновление стратегий
};

// Или event-driven с debouncing
class EventDrivenLearning {
  constructor() {
    this.pendingAnalysis = new Map();
    this.analysisDebounceMs = 60 * 60 * 1000; // 1 час
  }
  
  onKnowledgeAdded(topic) {
    // Не анализировать сразу, а накопить batch
    this.pendingAnalysis.set(topic, Date.now());
    
    if (!this.analysisScheduled) {
      this.analysisScheduled = setTimeout(
        () => this.runBatchAnalysis(),
        this.analysisDebounceMs
      );
    }
  }
}
```

---

#### 7. Накопление Технического Долга в Оценках
**Категория:** Интеграционная

**Как проявится:**
- Self-Assessment начинает собирать feedback
- Feedback противоречивый, некоторые оценки явно ошибочны
- Meta-Learning обучается на "грязных" данных
- Рекомендации ухудшаются со временем

**Когда проявится:**
- Месяц 3-6: Постепенное ухудшение качества

**Как предотвратить:**
```javascript
class FeedbackQualityGate {
  async validateFeedback(feedback) {
    const checks = {
      // Не принимать feedback сразу после добавления
      maturity: Date.now() - feedback.topicCreatedAt > 7 * 24 * 60 * 60 * 1000,
      
      // Проверять на outlier'ы
      statisticalValidity: this.isWithinNormalDistribution(feedback),
      
      // Конфликтующий feedback требует разрешения
      conflictResolution: await this.resolveConflicts(feedback)
    };
    
    return Object.values(checks).every(c => c);
  }
}
```

---

#### 8. Масштабирование Базы Знаний
**Категория:** Производительность

**Как проявится:**
- 6 месяцев × 10 тем/день = 1800 тем
- Поиск замедляется: 200ms → 2s
- Hybrid Search даёт нерелевантные результаты
- Пользователь возвращается к Google

**Когда проявится:**
- Месяц 4-6: Постепенное замедление

**Как предотвратить:**
```javascript
class TieredKnowledgeBase {
  constructor() {
    // Hot: последние 100 тем, в памяти
    this.hotCache = new LRU(100);
    
    // Warm: часто используемые, Redis
    this.warmCache = new RedisCache();
    
    // Cold: остальное, Qdrant + Memgraph
    this.coldStorage = new HybridSearch();
  }
  
  async search(query) {
    // 1. Проверить hot cache (1ms)
    const hot = await this.hotCache.get(query);
    if (hot) return hot;
    
    // 2. Проверить warm cache (10ms)
    const warm = await this.warmCache.get(query);
    if (warm) {
      this.hotCache.set(query, warm);
      return warm;
    }
    
    // 3. Поиск в cold storage (100-500ms)
    const cold = await this.coldStorage.search(query);
    this.warmCache.set(query, cold, '1h');
    return cold;
  }
}
```

---

### 🟢 Низкий Риск (Принять/Мониторить)

#### 9. Пользовательский Fatigue от Feedback Запросов
**Категория:** UX

**Как проявится:**
- Система постоянно спрашивает "Помогло ли это?"
- Пользователь игнорирует или отвечает случайно
- Feedback становится шумом

**Когда проявится:**
- Неделя 2-4: Сразу заметно

**Как предотвратить:**
- Sampling: спрашивать 1 из 10 случаев
- Passive signals: время на странице, scroll depth, copy/paste
- Explicit feedback только для спорных случаев

---

#### 10. Конфликт с Существующими Компонентами
**Категория:** Интеграционная

**Как проявится:**
- Frequency Tracker добавляет тему
- Gap Detector добавляет ту же тему
- Intent Detector добавляет ту же тему
- Три дубля в очереди

**Когда проявится:**
- Неделя 1: Сразу

**Как предотвратить:**
- Centralized deduplication layer
- Shared topic registry с TTL

---

## Альтернативные Подходы

### Если Adaptive Threshold Слишком Сложен

**Упрощённая версия:**
```javascript
// Два threshold вместо одного
const THRESHOLDS = {
  simpleQuestion: 0.75,  // Высокий для простых
  complexQuestion: 0.45  // Низкий для сложных
};

function detectGap(session) {
  const isComplex = session.text.length > 500 || 
                    session.codeBlocks > 0 ||
                    session.followUps > 2;
  
  const threshold = isComplex ? 
    THRESHOLDS.complexQuestion : 
    THRESHOLDS.simpleQuestion;
    
  return confidence < threshold;
}
```

### Если Полный Meta-Learning Слишком Рискован

**Ручная стратегия с подсказками:**
```javascript
class AssistedMetaLearning {
  async recommend() {
    // Система генерирует отчёт, но не решает автоматически
    const report = await this.generateReport();
    
    // Пользователь выбирает из 3 вариантов
    return {
      summary: report.summary,
      options: [
        { action: 'focus', target: report.topGap, label: `Focus on ${report.topGap}` },
        { action: 'balance', target: report.underrepresented, label: 'Balance categories' },
        { action: 'explore', target: report.newArea, label: `Explore ${report.newArea}` }
      ]
    };
  }
}
```

---

## MVP Версия: Что Сделать Сейчас

### Минимальный Gap Detector (1 неделя)
```javascript
class MinimalGapDetector {
  async detect(session) {
    // 1. Быстрый поиск в KB
    const searchResults = await knowledgeSearch(session.query);
    
    // 2. Простая эвристика: есть ли прямое совпадение?
    const hasDirectMatch = searchResults.some(r => 
      r.relevance > 0.85 && 
      r.type === 'exact_match'
    );
    
    // 3. Если нет прямого совпадения → gap
    if (!hasDirectMatch && this.isTechnical(session)) {
      return {
        detected: true,
        confidence: 0.6, // Фиксировано для MVP
        topic: this.extractTopic(session),
        reason: 'no_exact_match'
      };
    }
    
    return { detected: false };
  }
}
```

### Минимальный Meta-Learning (1 неделя)
```javascript
class MinimalMetaLearning {
  async analyze() {
    // Только базовые метрики, без ML
    return {
      // Самые частые gaps
      topGaps: await this.getMostFrequentGaps(10),
      
      // Темы, которые давно не использовались
      staleTopics: await this.getUnusedTopics(30),
      
      // Простая рекомендация
      recommendation: 'focus_on_top_gaps'
    };
  }
}
```

### План Внедрения

**Неделя 1-2: MVP**
- Запустить MinimalGapDetector на 20% сессий
- Собрать метрики: precision/recall на вручную размеченной выборке

**Неделя 3-4: Валидация**
- Если precision < 0.6 → доработать эвристики
- Если precision >= 0.6 → расширить до 100%

**Месяц 2-3: Итерация**
- Добавить adaptive threshold
- Запустить MinimalMetaLearning
- Weekly review с пользователем

**Месяц 4-6: Масштабирование**
- Полный Gap Detector
- Полный Meta-Learning
- Автоматические рекомендации

---

## Метрики Успеха

### Gap Detector
| Метрика | MVP Цель | Полная Цель |
|---------|----------|-------------|
| Precision | > 0.6 | > 0.8 |
| Recall | > 0.5 | > 0.7 |
| False Positive Rate | < 0.3 | < 0.15 |
| Time to Available | < 24h | < 1h |

### Meta-Learning
| Метрика | MVP Цель | Полная Цель |
|---------|----------|-------------|
| Recommendation CTR | > 0.2 | > 0.4 |
| User Satisfaction | > 3/5 | > 4/5 |
| Coverage Balance | < 5:1 | < 2:1 |
| Diversity Score | > 0.3 | > 0.6 |

---

## Заключение

**Ключевые риски:**
1. Фиксированный threshold 0.6 → адаптивный threshold
2. Корреляция ≠ причинность → многомерные метрики
3. Длинный feedback loop → инкрементальное обучение

**Рекомендация:** Начать с MVP, валидировать на реальных данных, затем масштабировать.
